name: Profile Summary Cards

on:
  # 1. 每天 UTC 时间 18:00 自动运行 (北京时间凌晨 2 点)
  schedule:
    - cron: '0 18 * * *'
  # 2. 允许手动点击按钮运行
  workflow_dispatch:
  # 3. 推送代码时也运行 (可选，方便调试)
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    name: generate-github-profile-summary-cards

    steps:
      - uses: actions/checkout@v4
        with:
          persist-credentials: false
          fetch-depth: 0

      - uses: vn7n24fzkq/github-profile-summary-cards@release
        env:
          GITHUB_TOKEN: ${{ secrets.GH_TOKEN }}
        with:
          USERNAME: ${{ github.repository_owner }}
          BRANCH_NAME: main
          THEMES: default,radical,monokai,dracula,github_dark
          UTC_OFFSET: 8

      - name: Setup Git User
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"

      - name: Push Changes
        run: |
          git add .
          git commit -m "Update profile summary cards" || true 
          git push https://${{ secrets.GH_TOKEN }}@github.com/${{ github.repository }}.git HEAD:${{ github.ref }} --force
